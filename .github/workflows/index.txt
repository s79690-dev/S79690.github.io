<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nur Aida Shazriza | Portfolio</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #f0f4f8;
            --card-bg: #ffffff;
            --accent: #005b96; /* Deep Ocean Blue */
            --accent-soft: #e0f2fe;
            --text-main: #0f172a;
            --text-muted: #475569;
            --border: #cbd5e1;
            --radius: 28px;
            --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.04);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 24px;
        }

        .bento-grid {
            max-width: 1100px;
            width: 100%;
            display: grid;
            gap: 20px;
            grid-template-columns: repeat(4, 1fr);
            grid-template-rows: auto auto auto;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 30px;
            box-shadow: var(--shadow);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: var(--accent);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }

        /* Labels & Text */
        .label {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--accent);
            margin-bottom: 12px;
            display: block;
        }

        h1 { font-size: 1.8rem; font-weight: 700; line-height: 1.1; margin-bottom: 8px; }
        h2 { font-size: 1.3rem; font-weight: 600; margin-bottom: 6px; }
        p { font-size: 0.95rem; color: var(--text-muted); line-height: 1.6; }

        /* Layout Logic */
        .card-profile { grid-column: span 2; grid-row: span 2; background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); }
        .card-location { grid-column: span 1; }
        .card-tech { grid-column: span 1; grid-row: span 2; }
        .card-case { grid-column: span 1; }
        .card-hobby { grid-column: span 1; }
        .card-playlist { grid-column: span 4; flex-direction: row; align-items: center; }

        /* Profile Special Styles */
        .tagline {
            font-style: italic;
            color: var(--accent);
            font-weight: 500;
            margin-top: 10px;
        }

        .social-links { display: flex; gap: 10px; margin-top: 25px; }
        .social-links a {
            text-decoration: none;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--accent);
            background: var(--accent-soft);
            padding: 8px 16px;
            border-radius: 14px;
            transition: 0.2s;
        }
        .social-links a:hover { background: var(--accent); color: white; }

        /* Subject Pills */
        .subject-container { display: flex; flex-direction: column; gap: 10px; margin-top: 15px; }
        .subject-pill {
            background: #f1f5f9;
            padding: 10px 15px;
            border-radius: 12px;
            font-size: 0.85rem;
            border-left: 4px solid var(--accent);
            font-weight: 500;
        }

        /* Playlist UI */
        .player-info { display: flex; align-items: center; gap: 15px; flex: 1; }
        .play-circle {
            width: 44px; height: 44px;
            background: var(--accent);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            flex-shrink: 0;
        }
        .progress-track {
            height: 6px; width: 200px;
            background: #e2e8f0;
            border-radius: 3px;
            overflow: hidden;
        }
        .progress-fill { width: 60%; height: 100%; background: var(--accent); }

        /* Responsive */
        @media (max-width: 1024px) {
            .bento-grid { grid-template-columns: repeat(2, 1fr); }
            .card-playlist { grid-column: span 2; }
        }

        @media (max-width: 650px) {
            .bento-grid { display: flex; flex-direction: column; }
            .card-playlist { flex-direction: column; align-items: flex-start; gap: 20px; }
            .progress-track { width: 100%; }
        }
    </style>
</head>
<body>

<div class="bento-grid">
    <div class="card card-profile">
        <div>
            <span class="label">BSc. Food Science</span>
            <h1>NUR AIDA SHAZRIZA BINTI AZMAN</h1>
            <p>Year 1 Undergraduate</p>
            <p class="tagline">"Innovating food safety."</p>
        </div>
        <div class="social-links">
            <a href="#">LinkedIn</a>
            <a href="#">GitHub</a>
            <a href="#">UMT Portal</a>
        </div>
    </div>

    <div class="card card-location">
        <div>
            <span class="label">Campus Hub</span>
            <h2>UMT</h2>
            <p>Kuala Nerus, Terengganu</p>
        </div>
        <p style="font-weight: 600; color: var(--accent);">15:00 MYT</p>
    </div>

    <div class="card card-case">
        <div>
            <span class="label">Research Highlight</span>
            <h2>Starch Gelatinization</h2>
            <p style="font-size: 0.85rem;">Analyzing texture profiles of Kuih Lapis.</p>
        </div>
    </div>

    <div class="card card-tech">
        <span class="label">Academic Focus</span>
        <h2>Core Subjects</h2>
        <div class="subject-container">
            <div class="subject-pill">Food Chemistry</div>
            <div class="subject-pill">Microbiology</div>
            <div class="subject-pill">Excel for Science</div>
            <div class="subject-pill">Lab Analysis</div>
        </div>
    </div>

    <div class="card card-hobby">
        <div>
            <span class="label">Wellness</span>
            <h2>Evening Walks</h2>
            <p>Pantai UMT Waterfront</p>
        </div>
    </div>

    <div class="card card-playlist">
        <div class="player-info">
            <div class="play-circle">▶</div>
            <div>
                <h2 style="font-size: 1rem; margin: 0;">Scientific Focus</h2>
                <p style="font-size: 0.8rem;">Deep Work Instrumental</p>
            </div>
        </div>
        <div class="progress-track">
            <div class="progress-fill"></div>
        </div>
    </div>
</div>

</body>
</html>